[build-system]
requires = ["flit_core >=3.11,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Acu"
authors = [{name = "izpy", email = "maxfpav@gmail.com"}]
license = "MIT"
license-files = ["LICENSE"]
dynamic = ["version", "description"]
dependencies = [
    "llvmlite>=0.45.1",
    "setuptools",
    "termcolor>=2.0.0"
]

[project.scripts]
acu = "acu.driver:main"
