func add(a: Int, b: Int) Int:
    return a + b

func multiply(a: Int, b: Int) Int:
    return a * b

func sub(a: Int, b: Int) Int:
    return a - b

func main() Int:
    var x: Int = add(5, 3)
    var y: Int = multiply(4, 2)
    var z: Int = add(x, y)
    var c = sub(5, 2)
    return 0
